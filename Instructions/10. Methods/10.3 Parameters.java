// Parameters are placed inside the parentheses of the declaration and must state the data type as well as the parameter name.

// The below method takes in two String parameters, greeting and name.
// Parameters are treated like variables within the body of the method.

public static void exampleMethod(String greeting, String name) {
  System.out.println(greeting + " " + name);
}
