// Constructors

// In order to create an object (an instance of a class), we need a constructor method.
// The constructor is defined within the class.

// Let’s take a look at the Car class with a constructor.
// The constructor, Car(), shares the same name as the class:

public class Car {
  // Constructor method:
  public Car() {
    // Instructions for creating a Car instance
  }  
 
  public static void main(String[] args) {
 
  }
}
